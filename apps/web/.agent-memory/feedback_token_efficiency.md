---
name: Token efficiency protocol
type: feedback
---

User prefers token-saving behavior in every session: read only needed files/ranges, avoid long repeated outputs, summarize checks, keep diffs narrow, and do not reprint large logs unless necessary.

Do not over-explain. Avoid unsolicited verbose status tables, before/after summaries, long verification lists, or repeated progress narration. Default to concise updates and a short final answer unless the user explicitly asks for detail.
