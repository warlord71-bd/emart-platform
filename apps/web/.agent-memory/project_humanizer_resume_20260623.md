---
name: project-humanizer-resume-20260623
description: Long-form product humanizer state + how to resume hand-writing top-seller descriptions
metadata: 
  node_type: memory
  type: project
  originSessionId: 3eaa0496-6a88-4cae-bf60-e4905d433e56
---

Long-form product humanizer (800-word rich PDP descriptions) is a MANUAL, proposal-gated process — NOT scheduled (no cron/PM2). [[project_product_content_humanizer_20260601]]

**State as of 2026-07-01:** `_emart_humanized` = **364** (359 + batch19's 5). Claude hand-wrote **batch19** (`selfwrite_batch19.py`): BoJ Revive Serum 93018, Numbuzin No.2 PDRN 92914, Skin1004 Centella Ampoule 92884, Medicube Collagen Jelly 92854, Anua Heartleaf Toner 53306 — all live + revalidated. Fixed **GMC hard-fail 74259** ("prescription retinoids"→professional-strength). **Gate hardened** in `batch_topsellers.py` to the 2026-07-01 audit standard: ≥1 `<ul>/<ol>`, em-dash density ≤2.2/100w, 500-750 words, hard-ban prescription/miracle + em-dash auto-repair in `gmc_safe()`. Model/key now **env-overridable** (`OPENROUTER_MODEL`, auto-loads `/root/.openclaw/credentials/openrouter_default.json` `apiKey`). **Run list ready**: `active/need-ids-20260701.json` = **3,060 IDs** (prioritized by sales, holdout+skip excluded). **BLOCKER: OpenRouter paid credits exhausted (30/30.2 used), free models now deprecated** — funded top-up needed, then run `cd workspace/humanizer/impression-priority && export EMART_DB_PASSWORD=... && python3 batch_topsellers.py active/need-ids-20260701.json` (resumable, skips already-humanized, gate-rejects failures to JSONL). **Holdout integrity fix:** holdout stored as `/shop/slug` paths — bare-slug compare was a silent no-op; normalized now. **13/212 holdout products were already humanized in prior sessions (incl batch18 axis-y 58391)** — measurement holdout ~6% contaminated; exclude those 13 from GSC analysis. Audit (other agent, 3,624 products): 421 pass, 148 flagged-but-failing (mostly early cleanser batch: 2 H3s/no lists/short/em-dash-heavy — needs repair), 211 organic passes unflagged, 3,055 not humanized. Commit `203db2e`.

**UPDATE 2026-07-01 (later, this session):** `_emart_humanized` now = **560**. Flagged **183 organic passes** — reproduced via `residue_lint.lint()` over live content (score≥80+GMC+residue+identity clean, real brand/focus/type, holdout excluded) with reversible marker `_emart_humanized_source=organic-pass-20260701`; ID list at `impression-priority/active/organic-pass-ids-20260701.json`. (My 183 vs audit's 211 = ~28 holdout products the audit didn't exclude but I correctly must not flag.) **Reframed the backlog:** cross-referencing the 2,868 unflagged against `workspace/seo-review/agentic-score.jsonl` tiers → only 10 THIN + 601 PARTIAL are weak; 2,257 already STRONG/GOLDEN (avg 83). After removing hair/makeup/accessory/perfume/body-wash/masks, **humanizer-fit skincare backlog = 139 PARTIAL, nearly all 0-sales long-tail** — core/bestsellers essentially done. The "3,060 need-ids" figure is real by flag but mostly non-fit long-tail; the actionable list is `active/priority-queue-skincare-fit-20260701.json` (139), with `priority-queue-haircare-makeup-20260701.json` (196) needing a category-adapted prompt. **148 cleanser rework = owner-deferred ("do later for cleansers").** Launched detached engine run (`run_detached.sh 15`, free chain, gated auto-apply) but **free models are 429-throttled right now — a test gen couldn't finish 1 product in 3min.** Confirmed fast path remains a small paid top-up. Batch19 rollback: `rollback-batch19-20260701.json`; 74259: `rollback-74259-gmcfix-20260701.json`.

**State as of 2026-06-23 (PM update):** `_emart_humanized` flag = **108** products. Claude hand-wrote batches 17 (5) + 18 (5) this session — top-sales serum/sunscreen/cream/lotion/eye-cream (Tiam, CosRx Snail 92, Beauty of Joseon, MediPeel, Simple, Axis-Y, Mary & May, APLB, Jigott, Dr Althea). All applied to DB + ISR revalidated. Fixed a live GMC slip: product 57702 had "miracle" (even negated trips scanners) → reworded + re-applied. (Earlier counts this/prior day: 275 figure was a different flag snapshot; trust the live `SELECT COUNT(*)` = 108.) Priority brands per owner: medicube, anua, dr althea, cosrx, numbuzin, skin1004, carenel, 3w clinic, dabo, axis-y, the ordinary, cerave.

**Handoff to Hermes (2026-06-23):** owner will generate the REST via the **Hermes agent** on an open OpenRouter model, NOT by hand. Built a reusable content-class package for this — see [[project_humanizer_engine_20260623]]. Batch scripts 17/18 live at `workspace/humanizer/impression-priority/selfwrite_batch17.py` / `18.py`.

**Quality bar (owner-set):** full-length (~500-750 words), top-notch human-expert voice, UNIQUE per product (no shared template — vary openings/structure/headings), GMC-safe (never "treats/cures/heals/prescription/clinically proven/miracle"). Skip cleansers (already humanized). Focus serum/sunscreen/cream top sellers.

**How to resume:**
1. Targets = top `total_sales` products, title matching serum|sunscreen|cream|essence|ampoule|moistur|lotion|toner, EXCLUDING cleans/wash/foam/shampoo/soap/scrub/mask/lip/hair/powder/cushion/patch/sheet/kit/combo/set, not already `_emart_humanized`, not in GSC holdout (212 slugs in `workspace/audit/active/baseline-snapshot-2026-05-31.json` key `holdout_slugs`), not in script HOLDOUT {2591,2611,4064}.
2. Write descriptions in a `selfwrite_batchN.py` under `workspace/humanizer/impression-priority/`, importing `humanizer_impression_priority as H` and calling `H.apply_to_db(pid, html + H.DISCLAIMER)`.
3. Set env `EMART_DB_PASSWORD` (from `/var/www/wordpress/wp-config.php`).
4. After each batch: POST `/api/revalidate` with `x-revalidate-secret` header + `{"tag":"products"}`; then commit+push the script.
5. ~110 serum/sunscreen/cream top sellers still remain.

**Reversibility:** rollback snapshots at `workspace/humanizer/impression-priority/active/rollback-topsellers-20260623.json` (309 products' original post_content).

**Note:** OpenRouter is OUT OF CREDITS (402) — bulk model runs need a top-up; free models (nvidia nemotron-120b:free, google gemma-4-31b:free) work but lower quality than hand-written. `batch_topsellers.py` is the GMC-safe model-based driver if credits are topped up. [[project_openrouter_humanizer_key_20260601]]
