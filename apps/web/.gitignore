
# Credential files that must never be committed
*.pem
google-service-account.json
gsc-oauth-token.json
*-oauth-token.json
*-service-account.json

# Generated design system previews (DesignSync)
design-system/

# Video-engine generated reels (runtime artifacts, never commit)
/public/videos/
