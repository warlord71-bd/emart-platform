
# Credential files that must never be committed
*.pem
google-service-account.json
gsc-oauth-token.json
*-oauth-token.json
*-service-account.json

# Generated design system previews (DesignSync) — anchored to this dir only,
# NOT src/lib/design-system/ (real source: colors.ts, typography.ts, index.ts)
/design-system/

# Video-engine generated reels (runtime artifacts, never commit)
/public/videos/
