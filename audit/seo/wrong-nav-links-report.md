# Wrong Navigation Links Report

Date: 2026-04-30

Scope: report-only for the current confirmed gaps pass.

## Findings

- Header `Toners` points to `/category/toners-mists`.
- Header `Sensitivity` points to `/category/dryness-hydration`.
- No navigation code was changed in this pass because the latest `SEO_TODO.md` asked to focus first on confirmed gaps: query stripping, absolute canonicals, brand detail title format, and reports.

## Follow-Up

If the next pass expands to Job 2 navigation, verify whether `Sensitivity` should remain mapped to `dryness-hydration` or move to a dedicated frontend concern/category route.

