Run the session end protocol:

1. Review what was done this session (check git log, diff, and conversation context)
2. Update AGENT_BUS.md — move any active work entry to LAST COMPLETED
3. Append a session block to `apps/web/SESSION-LOG.md`:
   ```
   ## YYYY-MM-DD — Agent: Claude Code
   - **Did:** (bullet list of completed work)
   - **Blockers:** (any unresolved issues, or "none")
   - **Next:** (suggested next steps)
   ```
4. Update `workspace/TASKS.md` if any tasks were completed or new ones discovered
5. Show a final summary of what changed
